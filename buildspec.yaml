version: 0.1

phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn test
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
      - aws s3 sync target/WebApp.jar s3://jackpandi/
artifacts:
  files:
    - '**/*'
